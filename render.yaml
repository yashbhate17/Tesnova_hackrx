services:
  - type: web
    name: tesnova-insurance
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: GEMINI_API_KEY
        value: your_actual_gemini_api_key
